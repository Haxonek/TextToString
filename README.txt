The principle behind this is that if you're copying a lot of text into a program that contains quotation marks, returns,  (think url's, code, etc) then you can use the formatter to keep the formatting and be able to enter it into code.

The site is viewable at https://haxonek.github.io/TextToString/